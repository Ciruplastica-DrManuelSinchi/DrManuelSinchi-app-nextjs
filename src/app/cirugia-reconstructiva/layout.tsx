import ClientLayout from '@/app/components/layout/ClientLayout'

export default function CirugiaReconstructivaLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <ClientLayout>{children}</ClientLayout>
}
